# Python tips and tricks

# 7.4. Creating an executable file from a python app

# pyinstaller : A python package that converts your Python code to an executable. It's a cross-platform so
# it will create executable files for windows (if we run on windows), linux(if we run on linux) and mac(if we run on mac)

# to create executable file simply type pyinstaller file.py


# 7.5. Creating a setup file
# innosetup -  for windows
